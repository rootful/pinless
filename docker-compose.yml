version: '3'

services:
  pinless:
    container_name: pinless
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - URL=http://localhost:3000 # change to eg https://yourdomain.com